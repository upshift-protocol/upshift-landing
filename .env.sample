# August SDK
NEXT_PUBLIC_AUGUST_DIGITAL_API_KEY=""

# Upstash Redis — MUST be the SAME instance as upshift-app so the cached TVL
# (key: tvl:overview) stays in sync. Link via Vercel Storage tab or copy these
# two values from the upshift-app project's env.
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""
